# AnvelopePlus Backend

This NestJS service exposes REST APIs backed by PostgreSQL and TypeORM. Endpoints are currently available for products, orders, and users.
A seed script inserts a sample product if the table is empty.

## Useful commands

```bash
npm run build     # compile TypeScript
npm run start     # start compiled app
npm run seed      # insert sample product
```
